service location
{
        disable = no
        socket_type = stream
        protocol = tcp
        wait = no
        user = ctf
        type = UNLISTED
        port = 20005
        bind = 0.0.0.0
        server = /home/ctf/run
        per_source = 10
        rlimit_cpu = 20
}
