version: '3'
services:
  gamechanger:
    image: gamechanger
    build: ./src/
    restart: always
    environment:
      - TERM=xterm
    ports:
      - "9254:9254"

networks:
  default:
    driver: bridge
    ipam:
      config:
        - subnet: 172.16.1.0/24
