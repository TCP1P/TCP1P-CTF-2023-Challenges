version: "3"

services:
    babyheap:
        build: .
        restart: always
        ports:
            - "4267:4267"
        read_only: true

networks:
  default:
    driver: bridge
    ipam:
      config:
        - subnet: 172.16.1.0/24
