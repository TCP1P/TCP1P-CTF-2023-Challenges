# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "Final Consensus"
author: "Kisanak"
category: Cryptography
description: Alice and Bob are speaking in hex, but they seem to understand each other. Will they reach the final consensus?
value: 0
type: dynamic
extra:
    initial: 1000
    decay: 20
    minimum: 100

# Flags specify answers that your challenge use. You should generally provide
# at least one.
# Can be removed if unused
# Accepts strings or dictionaries
flags:
    - TCP1P{nothing_ever_lasts_forever_everybody_wants_to_rule_the_world}

# Provide paths to files from the same directory that this file is in
# Accepts strings
files:
    - peserta/chall.py

# The state of the challenge.
# This is visible by default. It takes two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"
